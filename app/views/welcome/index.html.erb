hej

<%= form_tag '/', method: 'get', id: "search" do %>
	<%= text_field_tag(:artist) %>
	<%= submit_tag('Search', class: "btn btn-success") %>
<% end %>




<% if @artist %>
	<% @artist.each do |a| %>
	
	<%= link_to a.name, artist_path(a.id) %> <br>

<% end %>
<% end %>








